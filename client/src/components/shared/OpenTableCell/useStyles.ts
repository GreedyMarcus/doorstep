import { makeStyles } from '@material-ui/core/styles'

/**
 * Custom React hook that spearates the Open table cell component styling.
 */
export default makeStyles(theme => ({
  tableCell: {
    width: theme.spacing(5)
  }
}))
