import { makeStyles } from '@material-ui/core/styles'

/**
 * Custom React hook that spearates the Planned visits page styling.
 */
export default makeStyles(theme => ({
  tableContainer: {
    padding: theme.spacing(0, 1)
  }
}))
